
## gstack
Use /browse from gstack for all web browsing. Never use mcp__claude-in-chrome__* tools.
Available skills: /office-hours, /plan-ceo-review, /plan-eng-review, /plan-design-review,
/design-consultation, /review, /ship, /land-and-deploy, /canary, /benchmark, /browse,
/qa, /qa-only, /design-review, /setup-browser-cookies, /setup-deploy, /retro,
/investigate, /document-release, /codex, /cso, /careful, /freeze, /guard, /unfreeze, /gstack-upgrade.
If skills aren't working, run: cd .claude/skills/gstack && ./setup

## Repository Context
Read `WORKSPACE_CONTEXT.generated.md` before scanning larger project documents.
It is generated from `fleet-command/workspace/workspace.manifest.json` and is the low-token repo summary for this workspace.
